INSERT INTO: Inserir valores ou colunas nas tabelas
- Sintaxe

    INSERT INTO nomeDaTabela (coluna1, coluna2, coluna_n)
	  VALUES (valor1, valor2, valor_n),
		       (valor1, valor2, valor_n),
		       (valor1, valor2, valor_n);

- Exemplo

    INSERT INTO TabelaA (coluna1)
	  SELECT coluna 2
	  FROM TabelaB

UPDATE: Muda/atualiza os dados de uma tabela
- Sintaxe

    UPDATE Tabela
	  SET coluna1 = valor
	      coluna2 = valor
	  WHERE condição

- Exemplo:  
    
    UPDATE Video
	  SET Nome = ‘TOP’
	  WHERE VideoID = 1

DELETE: Deleta linhas da tabela
- Sintaxe

    DELETE FROM Tabela
    WHERE condição

- Exemplo: 

    DELETE FROM Video
    WHERE Nome = ‘TOP’

TRUNCATE: Exclui todos os dados da tabela, mantendo sua estrutura (colunas não são excluídas)
- Sintaxe

    TRUNCATE Tabela
